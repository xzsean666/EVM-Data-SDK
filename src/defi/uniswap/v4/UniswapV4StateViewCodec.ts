export const UNISWAP_V4_STATE_VIEW_SLOT0_SELECTOR = "0xc815641c";
export function encodeStateViewSlot0(poolId: string): string { if (!/^0x[0-9a-fA-F]{64}$/.test(poolId)) throw new Error("Invalid Uniswap V4 poolId."); return `${UNISWAP_V4_STATE_VIEW_SLOT0_SELECTOR}${poolId.slice(2).padStart(64, "0")}`; }
export function decodeUniswapV4StateViewSlot0(data: string): { readonly sqrtPriceX96: bigint; readonly tick: number; readonly protocolFee: number; readonly lpFee: number } { if (!/^0x[0-9a-fA-F]{256}$/.test(data)) throw new Error("Invalid Uniswap V4 StateView response."); const word = (i: number) => BigInt(`0x${data.slice(2 + i * 64, 2 + (i + 1) * 64)}`); const sqrt = word(0); if (sqrt <= 0n || sqrt >= 1n << 160n) throw new Error("Invalid sqrt price."); const signed = word(1); const tickRaw = Number(signed & ((1n << 24n) - 1n)); const tick = (tickRaw & 0x800000) ? tickRaw - 0x1000000 : tickRaw; if (tick < -8388608 || tick > 8388607) throw new Error("Invalid tick."); const protocolFee = Number(word(2)); const lpFee = Number(word(3)); if (protocolFee > 0xffffff || lpFee > 0xffffff) throw new Error("Invalid fee."); return Object.freeze({ sqrtPriceX96: sqrt, tick, protocolFee, lpFee }); }
